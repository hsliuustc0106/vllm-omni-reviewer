# PR #1567: [Bugfix] Allow to enable HSDP alone

Fixes standalone HSDP mode (fully_shard_degree > 1, all other parallelism = 1). Adds hsdp_replicate_size param, detects standalone HSDP, adjusts dit_parallel_size for correct rank generation. Missing unit test for is_standalone_hsdp detection. Test command provided shows working 4-GPU HSDP.
