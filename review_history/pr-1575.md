# PR #1575: [Benchmark] Add quantization quality benchmark script (LPIPS)

Benchmark script for LPIPS quantization quality measurement. Useful for standardizing quantization PR evaluations. Issues: (1) _unload_omni doesn't free memory - caller refs remain, causes OOM in ablations, (2) memory measurement from client process doesn't capture worker process memory. Needs fixes before merge.
