# PR #1613: perf: replace O(n^2) queue.remove() with O(n) single-pass partition in chunk scheduler

jt>
**Author:** dubin555
**Status:** OPEN
**Type:** Performance

## Summary
No critical issues found. This is a well-executed performance PR with comprehensive tests, clear benchmarks, and verified equivalence between original and fixed implementations.

## Changes
- Refactored `_process_chunk_queue` in `chunk_transfer_adapter.py` from O(n^2) to O(n)
- Added comprehensive test file `test_fix_on__queue_remove.py`

## Review Outcome
Approved. The implementation correctly transforms the O(n^2) queue.remove() pattern into an O(n) single-pass partition while preserving identical behavior.
